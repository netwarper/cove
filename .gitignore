# Encrypted user data — never commit
/data/
data/
*.enc
vault.json
meeting-notes-viewer.html

# Local environment overrides
.env

# Data-directory pointer (points at where THIS install keeps its data)
datadir.path

# OS / editor cruft
.DS_Store
Thumbs.db
node_modules/
*.log
.screenshots/
